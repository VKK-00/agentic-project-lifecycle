# Routing and gates

Use phases `orientation → discovery → specification → solution design → planning → implementation → staged release → operations`. Advance only when the current outcome, owner, approval, evidence, and residual risk are explicit. For greenfield work, start with problem validation and a walking skeleton. For brownfield, rescue, release, or audit requests, route directly to the matching specialist after orientation.
