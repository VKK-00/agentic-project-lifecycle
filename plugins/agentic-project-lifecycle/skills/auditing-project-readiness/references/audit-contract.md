# Audit contract

A validator pass proves only the assertions it checked. Report scope, inputs, commit or artifact versions, commands, exit codes, limitations, blockers, owners, and corrections. Never infer production readiness from document presence or stale evidence.
