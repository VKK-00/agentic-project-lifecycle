# Migration patterns

Prefer adapters, seams, strangler slices, and expand-contract changes. Define compatibility windows, dual-read/write rules where required, reconciliation, cutover criteria, and rollback for each independently deployable slice.
