# Tenancy, billing, and entitlements

Treat identity, tenant membership, authorization, and plan entitlements as separate layers. Define data isolation and lifecycle. Model billing as an idempotent state machine synchronized with a named source of truth; include retries, reconciliation, recovery, and account transitions.
